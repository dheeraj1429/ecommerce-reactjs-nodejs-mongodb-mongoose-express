const blogData = require('../models/blogModel');

// create a blog
const postBlog = async function (req, res) {
   try {
      console.log('done');
   } catch (err) {
      console.log(err);
   }
};

module.exports = {
   postBlog,
};
